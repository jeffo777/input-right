# Public-facing URLs for the open-source frontend application.
# These variables must be prefixed with NEXT_PUBLIC_ to be accessible in the browser.

# The URL of the open-source token server (running on port 8002 by default)
NEXT_PUBLIC_TOKEN_SERVER_URL="http://localhost:8002"

# The WebSocket URL of your LiveKit server
NEXT_PUBLIC_LIVEKIT_URL=